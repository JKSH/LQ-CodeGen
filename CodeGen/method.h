#ifndef METHOD_H
#define METHOD_H
/*
#include <QStringList>
#include <QVector>

struct Param
{
	QString name;
	QString type;
};

struct Methodd
{
public:
	Methodd(const QString& name, const QString& retType) :
		_name(name),
		_retType(retType)
	{}

	QString name() const
	{ return _name; }

	void addParam(const Param& param)
	{
		_params << param;
	}

	bool _isConstructor;
	QString _name;
	QString _retType;
	QVector<Param> _params;
};
*/
#endif // METHOD_H
